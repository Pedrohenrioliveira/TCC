using System; using MySql.Data.MySqlClient; class Program { static void Main() { using var conn = new MySqlConnection("Server=localhost;Port=3306;Database=baseapi_db;User=root;Password=root;"); conn.Open(); using var cmd = new MySqlCommand("UPDATE Partidas SET Status = 1, GolsMandante = NULL, GolsVisitante = NULL", conn); cmd.ExecuteNonQuery(); using var cmd2 = new MySqlCommand("UPDATE Classificacoes SET Pontos=0, PartidasJogadas=0, Vitorias=0, Empates=0, Derrotas=0, GolsPro=0, GolsContra=0", conn); cmd2.ExecuteNonQuery(); Console.WriteLine("Reset completo!"); } }
